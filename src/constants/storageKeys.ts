/**
 * localStorage 등 영구 저장소에 쓰는 키 문자열.
 * 값은 `UserProgress` JSON 한 덩어리(`version` 3: 닉네임·연속일·코인·등급·완료 Day·저장 단어 등).
 */
export const STORAGE_KEY_USER_PROGRESS = 'hadesvoca:userProgress' as const
export const STORAGE_KEY_AUDIO_SETTINGS = 'hadesvoca:audioSettings' as const
