# HadesVoca 프로젝트 분석 보고서

작성일: 2026-05-15

## 0. 요약

현재 프로젝트는 React 19 + TypeScript + Vite 기반의 모바일 우선 학습 MVP입니다. 서버 없이 `public/content`의 정적 JSON 콘텐츠를 불러오고, 사용자 진행 상태는 `localStorage`의 `hadesvoca:userProgress`에 저장하는 구조입니다.

빌드와 린트는 통과합니다.

- `npm.cmd run build`: 성공
- `npm.cmd run lint`: 성공

현재 완성도는 "Stage 1 단어 학습 + Stage 1 회화 학습 + 로컬 진행 저장" 기준으로 MVP 핵심 흐름은 동작 가능한 상태입니다. 다만 콘텐츠 확장, 복습 UX, 오답 해결 처리, PWA 오프라인 캐시, QA 자동화는 다음 단계에서 보강이 필요합니다.

## 1. 현재 완성된 핵심 기능

### 1.1 앱 구조와 라우팅

- `BrowserRouter` 기반 SPA 라우팅이 구성되어 있습니다.
- 시작 화면, 온보딩, 홈, 단어 학습, 회화 학습, 단어장, 오답노트 라우트가 연결되어 있습니다.
- 닉네임 기반 게스트 온보딩 가드가 구현되어 있습니다.
- 모바일 레이아웃과 하단 탭 내비게이션이 적용되어 있습니다.

주요 라우트:

- `/`: 시작 화면
- `/onboarding`: 닉네임 온보딩
- `/home`: 홈 대시보드
- `/word-study`: 단어 Day 목록
- `/word-study/:dayId`: 단어 퀴즈
- `/word-study/:dayId/result`: 단어 결과
- `/word-study/review`: 단어 복습
- `/conversation`: 회화 Day 목록
- `/conversation/:dayId`: 회화 플로우
- `/conversation/:dayId/result`: 회화 결과
- `/vocabulary-book`: 단어장
- `/wrong-note`: 오답노트

### 1.2 정적 콘텐츠 로딩

콘텐츠는 `public/content` 아래 JSON으로 분리되어 있고, 앱은 `fetch`로 읽습니다.

현재 콘텐츠 수량:

- 단어 Stage 1: 7 Days
- 단어 수: 21개
- 단어 문제 수: 63개
- 회화 Stage 1: 7 Days
- 회화 각 Day: 내레이션, 대화, 핵심 표현, 퀴즈, 컷신 이미지 포함

콘텐츠 검증 로직도 구현되어 있습니다.

- `stage-metadata.json` 스키마 검증
- 단어 콘텐츠 스키마 검증
- 회화 stage index/day 파일 조합 로딩
- 퀴즈 선택지와 정답 ID 검증

### 1.3 사용자 진행 저장

서버 없이 `localStorage` 기반 진행 저장이 구현되어 있습니다.

저장되는 핵심 데이터:

- 닉네임
- 로컬 사용자 ID
- 연속 학습일
- 마지막 학습일
- 코인
- 일일 코인 지급일
- 레벨/EXP
- 완료한 단어 Day
- 완료한 회화 Day
- 저장한 단어
- 저장한 표현
- 오답노트
- 단어 복습 상태
- 보상 거래 이력
- 최근 학습 위치
- 오늘 학습 횟수

저장소 안전 처리도 비교적 잘 되어 있습니다.

- JSON 파싱 실패 시 기본값 복구
- schema version migration/sanitize
- 비정상 숫자/문자열/배열 필드 방어
- 보상 중복 지급 방지용 transaction history
- Strict Mode 중복 저장 방지용 sessionStorage nonce

### 1.4 홈 대시보드

홈 화면에서 다음 정보를 보여줍니다.

- 닉네임 환영 문구
- 연속 학습일
- 오늘 학습 횟수
- 누적 학습 단어
- 레벨/등급
- 오늘의 코인 지급 버튼
- 최근 학습 이어가기
- 복습 대상 단어 수
- 최근 오답 일부
- 주요 메뉴 이동

### 1.5 단어 학습

단어 학습의 핵심 흐름은 구현되어 있습니다.

- Stage 1 Day 목록 표시
- 순차 해금
- 코인 부족 시 시작 차단
- Day 시작 시 코인 차감
- 객관식 문제 진행
- 문제 유형:
  - 영어 단어 보고 뜻 고르기
  - 뜻 보고 영어 단어 고르기
  - 빈칸 채우기
- 선택 후 정답/오답 피드백
- 예문/해설 표시
- 결과 화면 이동
- 최초 완료 보상 지급
- Stage 최초 완료 보상 지급
- 완료 Day 재학습 가능
- 학습 중 단어장 저장/삭제

### 1.6 단어 복습

단어 복습의 기본 뼈대가 구현되어 있습니다.

- 학습 완료/오답 기반 `wordReviewStatuses` 저장 구조
- 현재 완료 Day 기준 복습 대상 계산
- 복습 배너 표시
- `/word-study/review` 복습 세션 진입
- 복습 결과 반영
- 정답/오답에 따라 다음 복습 Day 계산

다만 복습 정책과 UX는 아직 MVP 수준입니다.

### 1.7 회화 학습

회화 학습도 Stage 1 기준 핵심 흐름이 완성되어 있습니다.

- Stage 1 회화 목록
- 순차 해금
- 컷신 이미지 표시
- 내레이션 단계
- 대화 스크립트 단계
- 핵심 표현 단계
- 표현 저장/삭제
- 표현 퀴즈 단계
- 결과 화면 이동
- 완료 기록 저장
- 오답 표현을 오답노트에 저장

### 1.8 단어장

단어장 기능이 구현되어 있습니다.

- 저장한 단어 목록
- 저장한 표현 목록
- 콘텐츠 JSON과 join해서 본문 표시
- 삭제 기능
- 탭 전환

저장소에는 본문을 저장하지 않고 참조 ID만 저장하는 구조라, 콘텐츠 수정과 사용자 데이터가 분리되어 있습니다.

### 1.9 오답노트

오답노트 기능이 구현되어 있습니다.

- 단어 오답과 표현 오답 분류
- 전체/단어/표현 탭 필터
- 오답 횟수
- 마지막 오답 날짜
- 정답 표시
- 콘텐츠 JSON과 join해서 문제 본문 표시

### 1.10 설정/백업

설정 시트에 다음 기능이 들어가 있습니다.

- 진행 데이터 JSON 내보내기
- 진행 데이터 JSON 가져오기
- 수동 저장 touch
- 도움말
- 저작권/고지
- 게스트 진행 초기화

## 2. 보강이 필요한 기능

### 2.1 콘텐츠 확장 체계

현재 앱은 Stage 1 중심으로 고정되어 있습니다.

보강 필요:

- Stage 2 이상 콘텐츠 로딩
- stage metadata 기반으로 단어/회화 Stage 목록 자동 생성
- 콘텐츠 import UI 실제 연결
- 콘텐츠 버전 관리
- 콘텐츠 누락/변경 시 사용자 진행 데이터와의 호환 정책

현재 `LearningPathStageImportBanner` 타입과 "준비중" UI는 있지만 실제 import 기능은 연결되지 않았습니다.

### 2.2 오답노트 해결 처리

오답노트에는 `resolved` 필드가 있지만, 사용자가 오답을 해결 완료 처리하거나 다시 풀어 해결하는 UX가 없습니다.

보강 필요:

- 오답 항목별 "해결됨" 처리
- 오답만 다시 풀기
- 오답 해결 시 `resolved=true` 반영
- 홈 최근 오답에서 해결된 항목 제외/관리
- 오답 빈도에 따른 우선순위 정렬

### 2.3 복습 기능 고도화

복습 데이터 구조는 있으나 사용자 경험은 아직 단순합니다.

보강 필요:

- 복습 주기 정책 확정
- 복습 대상 생성/해제 기준 명확화
- 복습 결과 히스토리 표시
- 복습 완료 후 단어장/오답노트와의 연동 강화
- 저장한 단어 기반 복습 진입
- 오답노트 기반 복습 진입

### 2.4 회화 학습 보상/성장 시스템

단어 학습에는 코인/EXP 보상이 있지만, 회화 Day 완료에는 보상이 없습니다. 코드 주석상 의도적으로 0 보상입니다.

보강 선택지:

- 회화 완료 EXP 지급
- 회화 퀴즈 정답률 기반 보상
- 회화 Stage 완료 보상
- 단어/회화 학습 간 성장 시스템 균형 조정

### 2.5 학습 통계 대시보드

현재 홈에는 기본 통계가 있지만 상세 분석은 부족합니다.

보강 필요:

- 최근 7일 학습 캘린더
- 단어/회화별 완료율
- 정답률 추이
- 오답 상위 단어/표현
- 복습 예정 수
- 코인/EXP 획득 내역

### 2.6 테스트 자동화

현재 빌드와 린트는 있지만 테스트 스크립트가 없습니다.

보강 필요:

- storage pure function 단위 테스트
- 콘텐츠 스키마 검증 테스트
- 학습 완료/보상 중복 방지 테스트
- 복습 상태 갱신 테스트
- 라우팅/주요 화면 smoke test
- PWA 서비스워커 캐시 테스트

### 2.7 접근성 및 모바일 QA

접근성 속성은 일부 적용되어 있으나 실제 시각 QA와 키보드 QA는 별도 검증이 필요합니다.

보강 필요:

- 모바일 해상도별 텍스트 넘침 점검
- 포커스 트랩/모달 접근성 점검
- 하단 탭/플로팅 시트 키보드 조작 점검
- 스크린리더 라벨 문구 정리
- 이미지 alt 정책 정리

### 2.8 PWA/오프라인 기능 정리

manifest와 service worker가 존재하지만, 현재 콘텐츠 구조와 일부 불일치가 있습니다.

보강 필요:

- 서비스워커 캐시 목록 최신화
- 회화 day 파일과 이미지 캐시
- Vite 빌드 산출물 캐시 전략 정리
- 캐시 버전 자동 갱신 전략
- 오프라인 첫 실행/재실행 정책

## 3. 기능적인 문제점

### 3.1 서비스워커가 존재하지 않는 회화 콘텐츠를 캐시하려고 함

`public/sw.js`의 `CONTENT_URLS`에 `/content/conversations/stage-1.json`이 들어가 있습니다. 현재 실제 회화 콘텐츠 구조는 다음과 같습니다.

- `/content/conversations/stage-1/index.json`
- `/content/conversations/stage-1/day-01.json`
- ...
- `/content/conversations/stage-1/day-07.json`

즉 `/content/conversations/stage-1.json` 파일은 없습니다.

영향:

- 프로덕션에서 service worker install 단계의 `cache.addAll()`이 실패할 수 있습니다.
- PWA 오프라인 캐시가 정상 설치되지 않을 수 있습니다.
- 첫 오프라인 진입/재방문 경험이 불안정해질 수 있습니다.

우선순위: 높음

### 3.2 완료한 단어 Day 재학습도 시작 비용을 차감함

단어 Day 상세 화면은 이미 완료한 Day를 replay로 허용하지만, 진입 시 `persistStartWordDayWithCoinCost()`를 호출합니다. 이 함수는 attempt key 기준으로 차감 중복을 막기 때문에 새로 재학습할 때마다 새 attempt로 간주되어 코인이 차감될 수 있습니다.

영향:

- "완료 Day 복습/재학습"과 "새 Day 시작 비용" 정책이 섞입니다.
- 사용자는 이미 깬 Day를 다시 볼 때도 코인을 잃을 수 있습니다.
- 학습 반복을 유도하기 어렵습니다.

정책 결정 필요:

- 완료 Day 재학습은 무료로 할지
- 모든 일반 Day 시도는 유료로 할지
- 별도 "복습" CTA만 무료로 둘지

우선순위: 높음

### 3.3 회화 세션 중 상태 변경을 렌더링 도중 수행하는 부분이 있음

회화 상세 페이지에는 Day 변경/빈 섹션 자동 스킵을 위해 렌더링 흐름 중 `setCurrentStep`, `setNarrationIndex`, `setDialogueIndex` 등을 호출하는 로직이 있습니다. 빌드와 린트는 통과하지만 React 권장 패턴에서는 위험한 구조입니다.

영향:

- 특정 데이터 조합에서 예측하기 어려운 재렌더가 발생할 수 있습니다.
- React 업데이트 정책 변화에 취약합니다.
- 복잡한 플로우 추가 시 버그가 늘어날 가능성이 있습니다.

우선순위: 중간

### 3.4 회화 결과 페이지 직접 진입 시 안내는 되지만 복구 경로가 제한적임

회화 결과는 `navigate(..., { state })`로 전달된 상태가 있어야 점수/완료 저장을 처리합니다. 직접 URL로 들어오면 안내만 보여줍니다.

현재 구조상 의도된 동작이지만, 사용자 경험 관점에서는 다음 보강이 필요합니다.

- "처음부터 다시 시작" 명확화
- 이미 완료한 Day라면 완료 결과 요약 표시
- state 유실 시에도 최소한 완료 여부는 localStorage에서 복구

우선순위: 중간

### 3.5 단어/회화 Stage 확장 시 하드코딩 지점이 많음

현재 MVP 상수로 Stage 1을 고정 사용합니다.

영향:

- Stage 2 추가 시 여러 화면 수정이 필요합니다.
- 콘텐츠 metadata의 장점을 충분히 활용하지 못합니다.
- 단어/회화 Stage 개념 확장 비용이 커집니다.

우선순위: 중간

### 3.6 저장소가 로컬 전용이라 기기 간 동기화가 없음

현재 의도적으로 서버 없는 MVP 구조입니다.

영향:

- 브라우저/기기 변경 시 진행 데이터가 이어지지 않습니다.
- localStorage 삭제, 시크릿 모드, 브라우저 정책 변경에 취약합니다.
- 백업/복원 UI는 있지만 사용자가 직접 관리해야 합니다.

우선순위: 제품 방향에 따라 중간 또는 낮음

### 3.7 콘텐츠 검증은 런타임 중심이고 개발 단계 검증 도구가 없음

앱 실행 중 fetch 후 schema assert는 있지만, 콘텐츠 JSON을 사전에 검증하는 CLI/test가 없습니다.

영향:

- 잘못된 콘텐츠가 배포 전 자동으로 잡히지 않을 수 있습니다.
- 콘텐츠 파일이 많아질수록 수동 검수가 어려워집니다.

우선순위: 중간

### 3.8 실제 브라우저 UI 검증은 아직 수행하지 않음

이번 분석에서는 빌드/린트/코드/콘텐츠 구조를 검토했습니다. 실제 브라우저에서 클릭 플로우와 화면 깨짐을 검증하지는 않았습니다.

추가 확인 필요:

- 온보딩부터 Day 완료까지 실제 클릭
- 코인 지급 후 단어 Day 진입
- 단어 결과 저장
- 복습 배너 생성
- 회화 Day 완료 저장
- 단어장/오답노트 표시
- 모바일 뷰포트 시각 QA

우선순위: 높음

## 4. 다음 작업 리스트

### 1순위: 안정성/버그 수정

1. `public/sw.js` 서비스워커 콘텐츠 캐시 경로 수정
2. 완료한 단어 Day 재학습 코인 차감 정책 결정 및 로직 수정
3. 회화 상세 페이지의 렌더 중 상태 변경 로직을 effect/reducer 기반으로 정리
4. 실제 브라우저 smoke test 수행
5. 오프라인/PWA 설치 동작 확인

### 2순위: 학습 기능 완성도

1. 오답노트 "해결됨" 처리 기능 추가
2. 오답노트에서 다시 풀기 기능 추가
3. 저장한 단어 복습 진입 추가
4. 복습 결과 히스토리와 다음 복습 예정 표시
5. 회화 완료 보상 정책 확정 및 적용

### 3순위: 콘텐츠 확장

1. stage metadata 기반 Stage 목록 렌더링
2. 단어 Stage 2+ 로딩 구조 적용
3. 회화 Stage 2+ 로딩 구조 적용
4. 콘텐츠 JSON 사전 검증 CLI 추가
5. 콘텐츠 에셋 누락 검사 추가

### 4순위: QA/운영 기반

1. storage 도메인 로직 단위 테스트 추가
2. 콘텐츠 스키마 테스트 추가
3. Playwright 기반 주요 플로우 테스트 추가
4. 모바일 레이아웃 스크린샷 QA
5. 백업/복원 실패 케이스 테스트

## 5. 권장 작업 순서

바로 다음 개발 사이클에서는 아래 순서를 권장합니다.

1. 서비스워커 캐시 경로 수정
2. 완료 Day 재학습 코인 차감 정책 수정
3. 브라우저에서 온보딩부터 단어 Day 완료까지 smoke test
4. 회화 Day 완료 smoke test
5. 오답노트 해결/다시풀기 설계
6. storage 테스트 추가

이 순서가 좋은 이유는 현재 앱이 이미 MVP 흐름은 갖췄기 때문에, 새 기능을 크게 붙이기 전에 "배포 후 깨질 수 있는 것"과 "사용자가 바로 불편해할 것"을 먼저 막는 편이 효율적이기 때문입니다.
